
import React from 'react';
import {
    View,
    Image,
    Text,
    StyleSheet,
} from 'react-native';

const Header = (props) => {
    return (
        <View>
        </View>
    );
}

const styles = StyleSheet.create({

});

Header.propTypes = {
}

export default Header;
